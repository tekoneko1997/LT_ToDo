import { connect } from 'react-redux';
import OATMButton from '../../../Components/Atoms/OpenAddTodoModalButton/OATMButton';


function mapStateToProps(state) {
    return state
}

function mapDispatchToProps() {
    return {
    };
}

export default connect(mapStateToProps, mapDispatchToProps)(OATMButton);